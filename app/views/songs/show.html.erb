<!DOCTYPE html>

<html>
  <head>
    <title><%= song.name %></title>
  </head>

  <body>
    <ul>
      <li><a href = <%= "../artists/#{song.artist.name}.html" %>>
          <%= "#{song.artist.name}" %>
      </a></li>
    </ul>
    <ul>
      <li><a href = <%= "../genres/#{song.genre.name}.html" %>>
        <%= "#{song.genre.name}" %>
      </a></li>
    </ul>
  </body>
</html>
